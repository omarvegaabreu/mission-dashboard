describe("Test GET /launches", () => {
  test("it should respond with 200 success", () => {
    const response = 200;
    expect(response).toBe(200);
  });
});

describe("Test POST /launches", () => {
  test("it should be 400 response", () => {});
  test("it should be 400 response", () => {});
  test("should be 200 success", () => {});
});
